# 计划前纵向影响面分析不完整

日期: 2026-08-24
状态: 📏 待复核
范围: Codex 任务计划前的跨层影响面调查
优先级: P1

## 摘要

原问题已完成提示词治理：全局规则保留简洁门禁，`$managing-work-stages` 统一拥有详细纵向闭包，GUI 规则只保留前端差异项；机械验证和合成行为验收已通过，真实高风险任务中的效果仍待复核。

## 问题

计划前的调查经常停在直接修改点，没有沿权威定义、runtime validation、公开出口、生成产物、直接与间接消费者、生命周期、失败恢复和实际验证入口完成纵向闭环。实施后才出现计划外文件、重新确认和整轮验证，部分中间提交还会让 GUI 暂时无法启动。

## 证据

- 已确认的遗漏包括间接 DOM selector、旧 value 断言消费者、runtime validator、生成产物、公开出口、Vite alias 依赖的 schema、fixture 中的旧提交链断言、已删除模块的残余 import，以及测试、错误处理和生命周期失败恢复路径。
- `/Users/jiangsheng/.codex/sessions/2026/08/19/rollout-2026-08-19T13-16-49-01a01873-5de5-7dd0-8292-bb416113841c.jsonl` 第 1389–1410 行记录了实施后才暴露的纵向影响面遗漏。
- `/Users/jiangsheng/.codex/sessions/2026/08/17/rollout-2026-08-17T20-20-55-01a00faa-eebc-7ff3-a920-e0bd90d4146e.jsonl` 第 852–866、1218–1224、1838–1853 行记录了间接消费者、生成链或失败恢复边界在计划后才被发现的代表事件。
- 月度审计保守统计该问题至少涉及 8 条已完成任务链。
- `6e53aaed4580ca9586d84ddeefc4a1b4cf1463af` 落盘并确认了纵向影响面提示词治理设计与实施计划。
- `9e6549ec2d48ee3c6069b31802cce8b92167c50e` 将全局规则压缩为风险、关键未知、精简证据摘要和复杂跨层反向审计四条门禁，并由 `$managing-work-stages` 路由到 `references/vertical-impact-closure.md`。
- `09e4e6390bf6762235547a2ec45b09d4bb1f002d` 将 `codex-gui/AGENTS.md` 收敛为 production/mount、export/alias、contract/validator、DOM/ARIA、生命周期和恢复等前端差异项。

## 判断

原始问题和根因判断仍然有效，但规则层缺口已经补齐。当前只能判定为“待复核”，不能标记“已修复”：提示词文本、结构验证和合成场景只能证明治理规则已经落地，尚不能证明后续真实高风险任务不会在计划确认后发现足以推翻根因、扩大计划或改变验证方式的新事实。

## 修复记录

- 2026-08-26：全局 `AGENTS.md` 只保留跨项目硬门禁，详细链路、六字段证据摘要、排除规则、计划映射和失败回退由 `$managing-work-stages` 的按需 reference 统一维护。
- 2026-08-26：`codex-gui/AGENTS.md` 删除通用未知阻断的第二 owner，只保留 GUI 特有纵向链路。
- 2026-08-26：独立反向审查发现并修正了“所有高风险任务都强制独立审计”和“全局规则与 skill 循环路由”两个候选问题；最终仅复杂跨层任务适用独立反向审计，委派机制仍由 `$delegating-micro-stages` 管理。

## 验证记录

- `$skill-creator` 的 `quick_validate.py` 按规定的隔离 `uv` 入口通过，输出 `Skill is valid!`；tracked 与 untracked 文件的空白和 allowlist 均单独核验。
- fnm-backed `pnpm run format:oxfmt` 通过，219 个匹配文件格式正确，且实际覆盖 `codex-gui/AGENTS.md`。
- 跨 owner 静态审查确认全局、主 skill、详细 reference 和 GUI 差异项不存在双 owner 或错误路由。
- 合成行为验收覆盖 contract 与生成链、DOM/ARIA selector、模块删除后的间接消费者、状态 owner 生命周期与失败恢复、验证命令未命中目标、不适用层的证据化排除，以及低风险局部改动反例。
- 上述验证没有替代真实任务效果复核；未运行产品代码测试、构建或远程 CI。

## 影响

计划会遗漏必要文件和验证，触发计划外范围确认、重新计划、重复验证和提交边界重拆；中间状态可能暂时破坏 GUI 可启动性，也会使用户难以判断原计划是否仍然可信。

## 后续处理

在后续真实高风险任务中复核：计划确认前是否稳定展示六字段证据摘要，且实施后是否不再出现足以推翻根因、扩大计划或改变验证方式的纵向影响事实。只有真实任务证据满足该条件后，才将状态更新为 `✅ 已修复`；若仍发现遗漏，按实际证据重新判断规则缺口，不把合成验收当作修复证明。
