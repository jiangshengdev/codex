# 执行图没有形成有效并行

日期: 2026-08-24
状态: 🟡 部分完成，提示词治理已落地，运行时效果待复核
范围: 多代理执行图、ready set、资源锁与关键路径调度
优先级: P2

## 摘要

提示词层已经建立描述式 DAG、动态 ready set、资源锁、失败域和最终并行证据规则；是否能在后续真实任务中稳定缩短关键路径，仍需新会话运行证据复核。

## 问题

当前执行流程曾把微阶段拆分误解为逐节点串行等待：独立的读取、解析和验证被逐文件执行，多个代理集中审查同一份 diff，而真正可独立推进的节点仍被无依据延后。

是否能够并行没有由当前 ready set 决定，Git index 等短时独占资源也被错误扩大为整个工作流的串行约束。结果是子代理更像需要主代理逐次驱动的远程命令执行器，而不是持续消费可执行节点的 worker。

## 证据

- 月度审计保守确认“子代理权限和调度不受控”至少涉及 5 条任务链。
- 处理 9 个生成物时，7 个彼此独立的文本验证被逐文件串行处理，形成接近 18 次往返：`/Users/jiangsheng/.codex/sessions/2026/08/20/rollout-2026-08-20T07-16-02-01a01c4f-6b8a-73f2-bd2c-954dad1618e2.jsonl` 第 1171–1212 行。
- 一个写集合与接口语义均独立的 Task 4，仍等待前三个任务全部提交后才启动，被无依据延后约 2 小时：rollout `01a02d92…` 第 232–243 行。
- 多个代理曾集中审查同一份 diff，没有形成关键路径上的有效 fan-out。
- 月度审计据此判断：“调用了很多子代理”不等于“有效并行”；并发必须由依赖、读写集合和资源锁决定。
- `codex-config` 提交 `87f28dec5808d8a934a0ace1793b27d7d321e79d` 已将执行期完整执行图收窄为“执行已落盘计划文件”时强制启用，并补充动态 ready set、关键路径、最小资源冲突、失败域和终态三项证据规则。
- 同一提交只修改 `AGENTS.md`、`skills/managing-work-stages/SKILL.md`、`skills/delegating-micro-stages/SKILL.md` 与 `skills/delegating-micro-stages/references/execution-graph.md`；前端提示词、产品代码、工具运行时和 `project-doc-workflow` 未修改。
- 合并后 `managing-work-stages` 与 `delegating-micro-stages` 的结构验证均成功；六个隔离合成案例及独立 oracle 审查全部通过，覆盖落盘计划、聊天计划、计划文件编写、普通委派、无并行空间和非成功终态。

## 判断

问题已在提示词治理层部分解决：全局入口、阶段 owner、委派 owner 与详细执行图契约已经形成一致的触发链，并明确要求基于真实依赖、读写集合和 canonical 资源锁动态调度。

当前不能判定为“已修复”。这些规则属于提示词层约束，不提供工具级 DAG enforcement、真实资源锁或不可伪造的运行时审计；结构验证和合成案例也不能证明新会话中的真实任务会持续产生有效并行。

## 修复记录

- 2026-08-27：提交 `87f28dec5808d8a934a0ace1793b27d7d321e79d` 落地执行图提示词治理；相关设计与计划由 `5d48d9a3eca54061a790060a5a18f79e9fe3d98d` 落盘，基线修正由 `5e83ab5936364fc0c4202a6be9cfbe553d687f2f` 记录。
- 执行图继续使用供大语言模型消费的描述式 DAG，禁止 Mermaid、ASCII 连线图、流程图、时序图及其他可视化；本次治理没有引入图形交付物。

## 验证记录

- 合并后的两个 changed skill 均通过规定的隔离 `quick_validate.py` 验证。
- 六个独立合成行为案例及未参与案例的 oracle 审查全部通过。
- 上述结果证明文件结构、触发边界和合成语义符合设计；尚未形成新会话中执行真实落盘计划的运行时证据。

## 影响

- 独立任务被不必要地阻塞，增加端到端完成时间和对话往返。
- 微阶段越细，若没有执行图，调度开销反而越大。
- 主代理上下文被大量节点级状态占用，更容易发生压缩和重复调查。
- 共享 Git index 等局部资源冲突会被误判为整个任务不可并行。

## 后续处理

在新会话中执行真实落盘计划，记录节点实际开始与完成时间、ready 节点延迟原因、canonical 资源冲突和实际关键路径，确认独立节点确实发生重叠且缩短关键路径。取得该行为证据后再判断是否标记为 `✅ 已修复`；若仍出现无依据串行或重复审查，应将实际失败记录作为后续提示词或工具级 enforcement 的新输入。
