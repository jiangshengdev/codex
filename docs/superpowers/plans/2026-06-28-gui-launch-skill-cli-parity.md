# GUI Launch Skill CLI Parity Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a normal `GUI 启动` skill that matches CLI `/gui` output exactly, without invoking the debug-responsive GUI workflow.

**Architecture:** Create a separate `gui-launch` skill for normal GUI URL printing and keep `debug-responsive-gui` scoped to explicit debugging, responsive, screenshot, and browser-verification tasks. The normal skill calls the outer `launch_gui` tool and prints every returned URL entry in the same label-aligned text format as CLI `/gui`; it does not choose LAN, Local, or VPN.

**Tech Stack:** Codex skills, Markdown `SKILL.md`, `init_skill.py`, `quick_validate.py`, `rg`.

---

## File Structure

- Create: `.codex/skills/gui-launch/SKILL.md`
  - Defines the ordinary GUI launch workflow.
  - Triggers on `GUI 启动`, `启动 GUI`, `/gui`, and requests for ordinary GUI URL output.
  - Requires output parity with CLI `/gui`.
- Create: `.codex/skills/gui-launch/agents/openai.yaml`
  - Generated by `init_skill.py` from explicit interface metadata.
  - Keep the generated file unless validation or project conventions require removing it.
- Modify: `.codex/skills/debug-responsive-gui/SKILL.md`
  - Clarifies that normal GUI startup belongs to `gui-launch`.
  - Keeps this skill reserved for explicit debug, responsive, screenshot, browser opening, or browser verification tasks.
- Read only: `docs/superpowers/specs/2026-06-28-gui-launch-skill-cli-parity-design.md`
  - Defines the accepted behavior boundary.
- Read only: `codex-rs/tui/src/app/gui.rs`
  - Source of truth for CLI `/gui` output formatting.

## Task 1: Initialize the Ordinary GUI Launch Skill

**Files:**
- Create: `.codex/skills/gui-launch/SKILL.md`
- Create: `.codex/skills/gui-launch/agents/openai.yaml`

- [ ] **Step 1: Confirm branch and missing skill directory**

  Run:

  ```bash
  git branch --show-current
  test ! -e .codex/skills/gui-launch
  ```

  Expected: first command prints `dev`; second command exits 0.

- [ ] **Step 2: Initialize the skill with the skill-creator helper**

  Run:

  ```bash
  python3 /Users/jiangsheng/.codex/skills/.system/skill-creator/scripts/init_skill.py gui-launch --path .codex/skills --interface display_name="GUI Launch" --interface short_description="Print Codex GUI URLs like CLI /gui." --interface default_prompt="GUI 启动"
  ```

  Expected: `.codex/skills/gui-launch/SKILL.md` exists, and `.codex/skills/gui-launch/agents/openai.yaml` exists if the helper generated agent metadata.

- [ ] **Step 3: Replace the generated `SKILL.md` with the final content**

  Write this exact content to `.codex/skills/gui-launch/SKILL.md`:

  ```markdown
  ---
  name: gui-launch
  description: Ordinary Codex GUI launch URL printing. Use when the user says `GUI 启动`, `启动 GUI`, `/gui`, asks for normal GUI URLs, or wants the same output as the CLI `/gui` command. Do not use for debugging, responsive-mode checks, screenshots, browser automation, or visual verification.
  ---

  # GUI Launch

  ## 基本规则

  - 回复用户使用简体中文。
  - 这是普通 GUI 启动流程；行为必须和 CLI `/gui` 保持一致。
  - 只调用 Codex 外层 `launch_gui` 工具并打印返回的 URL 列表。
  - 不要运行 `.codex/skills/debug-responsive-gui/scripts/debug-responsive-gui.mjs`。
  - 不要启动或控制浏览器，不要使用 Playwright，不要进入响应式模式，不要截图，不要验证页面。
  - 不要启动、停止或管理 `codex-gui` Vite dev server。
  - 不要选择 LAN、Local 或 VPN；不要添加优先级、fallback、可用性判断或地址筛选。
  - URL、label、顺序和 token 必须来自本次 `launch_gui` 返回值；不要手写、猜测、拼接或复用旧 URL。

  ## 输出格式

  调用 `launch_gui` 后，按 CLI `/gui` 的文本格式打印全部返回条目：

  ```text
  • GUI URLs:
    <label>:<padding><url>
  ```

  具体规则：

  - 第一行固定为 `• GUI URLs:`。
  - 后续每个 URL 条目一行，格式为两个空格、label、冒号、padding、URL。
  - padding 使用 CLI `/gui` 的对齐语义：以返回条目中最长 label 宽度为准，让 URL 起始列对齐。
  - 如果只返回 `Local`，只打印 `Local`。
  - 如果返回 `Local`、`LAN`、`VPN` 或其他 label，全部按 `launch_gui` 返回顺序打印。
  - 最终回复不要额外添加解释、Markdown 链接、调试状态、验证结果或替代地址。

  示例：

  ```text
  • GUI URLs:
    Local: http://127.0.0.1:12345/?threadId=t#token=x
    LAN:   http://192.168.3.165:12345/?threadId=t#token=x
    VPN:   http://100.88.28.119:12345/?threadId=t#token=x
  ```

  ## 错误处理

  如果 `launch_gui` 失败，按 CLI `/gui` 的语义报告失败信息：

  ```text
  Failed to launch GUI: <error>
  ```

  不要自动切换到 debug skill，不要启动 Vite，不要打开浏览器。
  ```

## Task 2: Clarify the Debug Skill Boundary

**Files:**
- Modify: `.codex/skills/debug-responsive-gui/SKILL.md`

- [ ] **Step 1: Read the current debug skill**

  Run:

  ```bash
  sed -n '1,180p' .codex/skills/debug-responsive-gui/SKILL.md
  ```

  Expected: the skill describes browser-debug behavior using `playwright-cli` and Chrome for Testing.

- [ ] **Step 2: Update the frontmatter description**

  In `.codex/skills/debug-responsive-gui/SKILL.md`, set the description to:

  ```yaml
  description: Use only when debugging the Codex GUI with playwright-cli in a visible Google Chrome for Testing browser, including DevTools, responsive layout checks, screenshots, browser opening, visual verification, or reproducible browser-control step records. Do not use for ordinary `GUI 启动`, `启动 GUI`, `/gui`, or URL-only output; use gui-launch for those.
  ```

- [ ] **Step 3: Add a boundary bullet under `## 基本规则`**

  Add this bullet after `- 回复用户使用简体中文。`:

  ```markdown
  - 普通 `GUI 启动`、`启动 GUI`、`/gui` 或只需要打印 GUI URL 的请求必须使用 `gui-launch`；本 skill 只用于明确的 debug、响应式、截图、浏览器打开、视觉验证或可复现浏览器控制场景。
  ```

  Expected: ordinary GUI startup is no longer described as a debug workflow trigger.

## Task 3: Validate Skill Behavior and Boundaries

**Files:**
- Validate: `.codex/skills/gui-launch/SKILL.md`
- Validate: `.codex/skills/debug-responsive-gui/SKILL.md`

- [ ] **Step 1: Validate the new ordinary launch skill**

  Run:

  ```bash
  python3 /Users/jiangsheng/.codex/skills/.system/skill-creator/scripts/quick_validate.py .codex/skills/gui-launch
  ```

  Expected: exit code 0 with `Skill is valid!`.

- [ ] **Step 2: Validate the debug skill**

  Run:

  ```bash
  python3 /Users/jiangsheng/.codex/skills/.system/skill-creator/scripts/quick_validate.py .codex/skills/debug-responsive-gui
  ```

  Expected: exit code 0 with `Skill is valid!`.

- [ ] **Step 3: Confirm ordinary launch contains CLI parity and no URL selection policy**

  Run:

  ```bash
  rg -n -e 'CLI `/gui`|• GUI URLs|不要选择 LAN、Local 或 VPN|不要添加优先级' .codex/skills/gui-launch/SKILL.md
  rg -n -e '优先.*LAN|Local 回退|VPN 偏好|可用性判断|地址筛选' .codex/skills/gui-launch/SKILL.md
  ```

  Expected: first command finds the CLI-parity and no-selection rules; second command only matches the explicit prohibited phrases, not instructions to perform selection.

- [ ] **Step 4: Confirm debug skill points ordinary startup away**

  Run:

  ```bash
  rg -n -e 'gui-launch|普通 `GUI 启动`|只需要打印 GUI URL|Do not use for ordinary' .codex/skills/debug-responsive-gui/SKILL.md
  ```

  Expected: matches show ordinary startup is delegated to `gui-launch`.

- [ ] **Step 5: Check changed files**

  Run:

  ```bash
  git status --short
  git diff -- .codex/skills/debug-responsive-gui/SKILL.md .codex/skills/gui-launch/SKILL.md
  ```

  Expected: changed files are limited to the new `gui-launch` skill, the debug skill boundary update, and the accepted design/plan docs. Do not stage or commit.

## Stop Conditions

- Stop before creating files if the current branch is not `dev`.
- Stop if `.codex/skills/gui-launch` already exists and contains user changes; inspect before overwriting.
- Stop if `init_skill.py` fails; do not hand-create the skill directory as a substitute without user confirmation.
- Stop if the generated `agents/openai.yaml` contains invalid metadata or validation fails repeatedly.
- Stop if implementation requires changing CLI `/gui`, `launch_gui`, Vite, or browser automation code; that exceeds this plan.
- Do not stage or commit unless the user explicitly asks.

## Self-Review

- Spec coverage: the plan implements a separate ordinary GUI launch skill, preserves CLI `/gui` text format, forbids URL selection, keeps debug behavior separate, and validates both skills.
- Placeholder scan: no unresolved placeholder markers or unspecified implementation steps are present.
- Command consistency: every command uses existing repo paths or verified helper scripts, and no project script is referenced without checking its existence.
