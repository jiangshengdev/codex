# 01：断连保留新建会话输入区

**What to build：** 新建会话失去连接后继续显示目录条和禁用的输入框，已有草稿保持可见。顶部现有连接提示提供恢复及诊断入口，底部不再重复提示。恢复连接后按既有业务限制恢复操作，不自动发送。

**Blocked by：** 无其他产品任务；开始实施前须独立提交已确认的本次设计和计划。

**Status：** draft，等待用户确认实施。

- [ ] 有草稿、空草稿、离线进入新建页均保留输入区，断连期间不能编辑或发送。
- [ ] 草稿内容和固定目录在断连及恢复期间保持，目录完整路径仍可展开、选中及通过键盘访问。
- [ ] 顶部已有连接提示、重新连接与诊断入口保留，底部重复提示移除，不新增操作控件。
- [ ] 断连后不新增技能、创建或发送请求，失效连接的技能结果不影响当前可用状态。
- [ ] 重连不自动创建或发送，不绕过创建中、激活中和交接结果未知等已有业务限制。
- [ ] 既有发送及身份保留回归检查继续有效，测试旧提示断言按新契约替换，不放宽禁止重复发送等检查。
- [ ] 浅深主题与宽窄视口展示清晰，输入区不再因失联被替换为提示段落，目录弹出层无溢出和裁切。
- [ ] 完成计划要求的自动化、catalog 稳定性与独立审查；真实无头验收单列证据，缺失场景不得标通过。
- [ ] 在精确任务范围创建独立产品提交，保留文档提交边界，不修改其他页面和共享重连机制。

具体修改边界、验证入口、执行依赖和提交要求见同目录 plan.md。本 ticket 不单独授予实施或外部发布权限。
