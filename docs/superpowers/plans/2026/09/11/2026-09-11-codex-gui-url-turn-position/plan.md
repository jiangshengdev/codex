# URL 定位到 turn 结尾：任务计划

日期：2026-09-11

状态：用户已确认两个任务的粒度与依赖；已授权本地落盘。实现尚未授权，设计未决项与技术核验尚未闭合，本文不是可直接执行的最终计划。

## 来源与交付边界

- [设计规格](../../../../../specs/2026/09/11/2026-09-11-codex-gui-url-turn-position-design.md)
- [已确认决策](../../../../../research/2026/09/11/2026-09-11-codex-gui-url-turn-position-decisions.md)
- 用户对两个端到端任务的粒度及依赖回复“确认”；这不代表确认设计中的未决行为，也不授权开始实现或 Git 提交。

采用 `to-tickets` 的每任务一个文件及显式阻塞依赖结构，沿用本项目日期化计划目录；不发布远程 issue。本文为共同边界与依赖索引，不取代单独任务文件。

## 已确认任务与依赖

| 任务 | 前置实现任务 | 交付 |
| --- | --- | --- |
| [01：历史预览支持 URL 定位](01-history-turn-position.md) | 无 | 历史详情端到端定位，以及两类页面共用的定位能力 |
| [02：当前聊天接入 URL 定位](02-current-task-turn-position.md) | 01 | 当前聊天端到端定位，与 projection、输入框和原有滚动协调 |

任务 02 消费任务 01 的稳定共享定位接口与行为证据，因此该边是产物依赖，不是任务编号产生的串行。无需先做独立的大范围重构，不引入临时双路径或兼容层。各任务均包含其功能验收，不把测试单独拆成只能最后补齐的水平任务。

## 实施前共同门禁

以下事项仍未完成，两项任务均不得标记为 `ready-for-agent`：

1. 闭合参数缺项、空值、重复参数、未知 `position` 及其他非法输入的用户可见行为。
2. 闭合同页查询参数改变、浏览器前进后退与“一次打开”的关系。
3. 核验启动与刷新链路保留查询参数、原有滚动 owner 的协调方式、目标渲染就绪条件及输入框遮挡边界。
4. 根据这些证据补全精确修改范围、项目验证入口、生成物范围和执行节点能力信封，再取得实施授权。不得把未知项交给执行者自行选择产品行为。

其中只读技术核验可以独立于产品决策开展；当前确认仅落盘任务，不把本轮扩展为继续调查。

## 调度与提交边界

当前实现 ready set 为空：共同门禁尚未满足。门禁闭合后的首个功能任务为 01；02 等待 01 的稳定共享产物。最终组合验证消费两项任务的集成状态，不构成第三个功能 ticket。

功能关键路径为历史预览及共享定位、当前聊天接入、最终组合验证。没有已识别的独立实现分支需要 fan-out；最终验证为两项任务的 fan-in。此处只记录任务 DAG，不提前启动执行图调度。

当前工作区为 `/Users/jiangsheng/cnb/codex`，当前分支核验为 `dev`。本轮不创建 worktree、不切换分支、不操作 Git index。后续执行前重新核验工作区状态；本计划未授权创建额外 worktree 或委派写入。

拟采用文档独立提交、任务 01 独立提交、任务 02 独立提交的边界，已有提交的修正另建提交，不 amend。文档提交必须在实施前完成；被 ignore 的文件不得强制暂存。以上记录未来执行约束，本轮没有提交授权。

精确执行节点、readSet/writeSet、资源锁、验证命令及能力信封待共同门禁闭合后按执行图契约补齐，不能仅凭本任务索引执行代码编辑。共享分页与路由的修改需协调同一工作树读写；Git index、生成器与验证 runner 由唯一执行 owner 管理，不能凭不同任务名推定隔离。

## 最终验收与范围

两条既定 URL 在各自页面满足结尾定位、跨页、缺失提示、刷新、参数保留及一次定位行为；无参数的既有体验保持不变。任务 02 完成后验证两类页面组合状态，防止共享能力的改动破坏历史预览。

自动化优先复用页面级 Browser 测试，验证可见结果与实际几何关系。真实运行时直接打开与刷新需要单独证据，不能用自动化结果冒充。默认无头；可见桌面验收需另获明确授权。

不恢复 fork 来源入口，不增加消息级或 `position=start` 定位，不提供复制链接入口，不新增延迟加载、Rust 协议或持久化字段。若证据表明既定范围无法完成目标，暂停受影响部分并说明差异，不暗中扩大范围。
