# 01：中途订阅协作活动完整恢复

**What to build:** 用户中途打开会话时，立即看到仍在进行中的 `wait`、`resumeAgent` 活动；完成后原位显示真实结果，与持续订阅一致。快照已覆盖的重复事件不新增记录。

**Blocked by:** None（无其他功能 ticket 阻塞；执行仍需计划确认及独立文档提交）。

**Status:** draft（已落盘，待计划确认；不是已发布的 ready-for-agent issue）。

- [ ] 进行中快照复用已有活动展示及 receiver 条件。
- [ ] 后续完成事件不会仅因 item ID 出现在快照而被忽略。
- [ ] 成功和失败完成均正确更新同一条活动，不遗留进行中状态。
- [ ] 重复完成与已完成快照重放不会产生重复记录或状态倒退。
- [ ] 两种协作工具都有从真实重放分类到 transcript 输出的完整回归，与持续订阅对照。
- [ ] 原有会话隔离、提交连续性、分页和 chunk 稳定性保持。
- [ ] 定向自动化、项目 CI、独立审查及真实无头验收完成。
- [ ] 不改变后台执行、协议、其他工具的展示规则，不新增兼容路径。
- [ ] 相关文档先独立提交；行为修改与验证记录随后形成独立本地提交。

测试采用既有生产接口和合法共享 fixtures，验证可观察行为，不锁定内部索引实现。具体范围和调度见同目录实施计划。
