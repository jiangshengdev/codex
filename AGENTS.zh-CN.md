# Codex GUI

- 编辑 `codex-gui/**` 下的文件前，先阅读 `codex-gui/AGENTS.md`，并遵守其中的前端专属规则。

# Rust/codex-rs

- 在 `codex-rs/**` 中开展工作前，先阅读 `codex-rs/AGENTS.md`，并遵守其中的 Rust 专属规则。

## Agent skills

### 问题追踪器

本仓库使用 CNB Issues。开始任务工作或操作问题追踪器前，先阅读 `docs/agents/issue-tracker.md`；CNB 不可用时也遵循其中的工作规则。

### Triage 标签

使用 `docs/agents/triage-labels.md` 中定义的五个默认 triage 状态标签。

### 领域文档

采用覆盖 GUI 与 Rust 后端的 single-context 布局。见 `docs/agents/domain.md`。
